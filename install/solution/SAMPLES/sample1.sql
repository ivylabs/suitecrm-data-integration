CREATE TABLE AGENT_LEADS
(
  LEAD_ID VARCHAR(36)
, LEAD_MODIFIED DATETIME
, LEAD_ASSIGNED_ID VARCHAR(36)
, LEAD_SOURCE VARCHAR(100)
, LEAD_STATUS VARCHAR(100)
, USERNAME VARCHAR(60)
, DEPARTMENT VARCHAR(50)
, YEAR INT
, QUARTER INT
, MONTH INT
, MONTH_DESC VARCHAR(9)
)
;CREATE INDEX idx_AGENT_LEADS_lookup ON AGENT_LEADS(LEAD_ID)
;
